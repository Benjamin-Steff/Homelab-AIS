## Déploiement automatique (PXE)

## 🎯 Objectif

Mettre en place un déploiement automatisé d’un système d’exploitation via le réseau (PXE boot), sans support physique.

Le client démarre sur le réseau et récupère automatiquement l’image ISO hébergée sur le serveur.

---

## 🏗 Architecture utilisée

- Windows Server (SRV-DC01)
- Rôle DHCP
- Rôle WDS (Windows Deployment Services)
- Réseau interne : 10.0.0.0/16

Client :
- Démarrage en mode PXE (Network Boot)

---

## 🔄 Fonctionnement global

1. Le client démarre
2. Il envoie une requête DHCP
3. Le serveur DHCP répond avec :
   - Une adresse IP
   - Les options PXE
4. Le client télécharge le programme de démarrage (boot file)
5. L’image d’installation est chargée depuis le serveur WDS
6. Installation automatique du système

---

## 📡 Configuration DHCP

Le serveur DHCP fournit :

- Adresse IP
- Masque
- Passerelle
- DNS
- Options PXE si nécessaires

Dans un environnement avec DHCP et WDS sur le même serveur :
- Aucune configuration spécifique d’options 66/67 n’est requise
- WDS intercepte automatiquement les requêtes PXE

---

## 🖥 Installation de WDS

Rôle installé :
- Windows Deployment Services

Configuration :
- Mode intégré à Active Directory
- Stockage des images sur D:\RemoteInstall
- Activation du serveur PXE

---

## 📀 Import d’image

Deux types d’images :

### Boot Image
Image WinPE (issue de l’ISO Windows)

### Install Image
Image du système à déployer

Images importées depuis l’ISO officiel Windows.

---

## 🚀 Déploiement

Le client :

- Boot en PXE
- Obtient IP via DHCP
- Charge WinPE
- Lance l’installation Windows

---

## 🔐 Sécurité

- Serveur autorisé dans Active Directory
- Réponse PXE limitée aux clients connus si nécessaire
- Déploiement possible avec fichier de réponse (unattended.xml)

---

## 🧠 Compétences démontrées

- Interaction DHCP / PXE
- Déploiement réseau automatisé
- Gestion d’images système
- Infrastructure centralisée
- Préparation environnement entreprise
